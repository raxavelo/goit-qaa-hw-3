// Exercise 1: Creating arrays

const fruits = ['apple', 'banana', 'orange', 'grape', 'watermelon'];

// Exercise 2: Arrays iteration

const numbers = [1, 4, 9, 13, 2, 5, 27, 2];

for (const number of numbers) {
    if (number > 5) {
        console.log(number);
    }
}